# Suppress warnings from unfixable transitive dependencies (e.g. drizzle-kit)
loglevel=error
# Suppress funding logs to keep console clean
fund=false
# Suppress audit logs for transitive dependencies
audit=false
